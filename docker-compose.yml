services:
  documind-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: documind_ai_app
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - PORT=8501
    restart: unless-stopped
